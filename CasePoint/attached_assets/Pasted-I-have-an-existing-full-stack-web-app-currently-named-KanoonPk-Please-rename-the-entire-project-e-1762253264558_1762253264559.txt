I have an existing full-stack web app currently named “KanoonPk.”
Please rename the entire project everywhere to “CasePoint.”

This includes all:

File and folder names

Flask and React project titles

Variables, constants, and comments

HTML titles and metadata

Component names and imports

Database references and admin labels

README documentation

Environment variables or config references

⚙️ Backend (Flask) Changes

Replace every instance of KanoonPk with CasePoint in:

app.py, config.py, and all routes

Blueprints and imports

Flask-Admin dashboard title → “CasePoint Admin”

Logging names or API messages

Docstrings, comments, and responses

Update any environment variable or prefix like KANOONPK_ → CASEPOINT_

Update app startup print messages, e.g.:

print("🚀 CasePoint backend running...")

🎨 Frontend (React) Changes

Rename project title in package.json → "name": "casepoint"

Update index.html <title> → “CasePoint – AI Legal Research Platform”

Change all UI branding text:

Navbar logo → “CasePoint”

Footer → “© 2025 CasePoint. All rights reserved.”

Landing page hero section → “Welcome to CasePoint”

Update all React components that import or display “KanoonPk” text.

Update favicon or logo file names (if they include “kanoonpk”).

Replace references in src/pages, src/components, and src/services.

🧩 Database and Config Updates

If a database name or schema uses kanoonpk, rename to casepoint.
Example:

ALTER DATABASE kanoonpk RENAME TO casepoint;


Update SQLALCHEMY_DATABASE_URI in .env if it contains old name.

Change Flask-Admin header:

admin = Admin(app, name="CasePoint Admin", template_mode="bootstrap3")


Update any table_prefix or identifier used in migrations.

🧱 Documentation and Meta

Update README.md title → # CasePoint

Replace project description text:
“KanoonPk is an AI-powered law research platform” →
“CasePoint is an AI-powered legal research and citation platform.”

Update installation commands, URLs, and screenshots to use “CasePoint.”

Update any Git repo URLs or metadata (if referenced).

💎 Quality Requirements

Do not break import paths or dependencies.

Preserve all original functionality and routes.

Maintain case-sensitive replacements (KanoonPk → CasePoint, kanoonpk → casepoint).

After renaming, ensure app builds and runs with no broken imports.

Test Flask backend and React frontend start commands:

python backend/app.py
npm run dev


Final print/log message:
✅ CasePoint successfully launched (formerly KanoonPk)

✅ Expected Output

A fully functional app globally renamed to CasePoint.

All code references, metadata, and UI text updated.

Project runs seamlessly with new name across both frontend and backend.

Clean, consistent branding everywhere (no trace of “KanoonPk”).