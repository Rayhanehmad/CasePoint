Add full integrated AI Case Analyzer feature to CasePoint.

FEATURES TO IMPLEMENT:

1. One user input (text narrative) should produce THREE outputs:
   - AI Counter Arguments
   - Related Case Laws (citations)
   - Related Rules / Acts / Statutes

2. ADD NEW FEATURE:
   - “Which Laws Apply?” button
   - This button analyzes user text and produces a clear list of:
       * Applicable Acts
       * Applicable Sections
       * Relevant Rules
       * Applicable Criminal/Civil provisions
   - This should be based on regex detection for now, but auto-upgrade when LegalStatute DB is added.
   - Show result in a separate UI box.

3. Backend:
   - Create API: /api/auto_counter_arguments
       * Input: narrative text
       * Output: AI defence counter arguments
   - Create API: /api/analyze_case
       * Detect citations: PLD, SCMR, MLD, YLR, CLD, CLC, PTD, PCrLJ, PLC
       * Match citations with LegalCitation DB
       * Detect statutes:
           - Section 302
           - CrPC 154
           - PPC 489-F
           - QSO 17
           - “XYZ Act 2020”
           - CPC Order 39 Rule 1
       * TRY to match detected rules/acts with LegalStatute model
       * If DB entry exists: return id, title, section, category, is_database_item=True
       * If DB empty: return raw text only, is_database_item=False
       * AUTO-UPGRADE when DB entries exist (no code changes later)

   - Create API: /api/which_laws_apply
       * Input: text
       * Output: list of applicable laws/sections
       * Detection must use the same regex rules as analyze_case
       * This endpoint returns all matched Acts/Rules/Statutes in simple list form

4. Frontend (React):
   - Create CaseAnalyzer.jsx
   - UI components:
       * Text input box for user narrative
       * Button 1: “Analyze Case”
       * Button 2: “Which Laws Apply?”
   - After "Analyze Case":
       * Show 3-column layout:
           COLUMN 1: Counter Arguments (AI)
           COLUMN 2: Related Case Laws (clickable)
           COLUMN 3: Rules / Acts / Statutes (clickable if DB entry exists)
   - After “Which Laws Apply?”:
       * Show a fourth section or modal:
           “Applicable Laws”
           - List all detected Acts, Sections, Rules
           - If database entry exists → clickable link /statute/:id
           - If not → show plain text

5. UI/UX Requirements:
   - Use CasePoint theme with Tailwind
   - Buttons must be:
       “Analyze Case”
       “Which Laws Apply?”
   - Layout should be clean, 3-column for full analysis
   - “Which Laws Apply?” result must appear below or in a separate highlight box

6. Integration Notes:
   - The system must work NOW even without Acts/Rules DB.
   - When LegalStatute entries are added later, the same code should immediately make results clickable.
   - No code rewriting required later.

Implement this EXACTLY.
