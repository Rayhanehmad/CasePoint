<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>CasePoint — Dashboard</title>

  <!-- Tailwind (Play CDN) – can live alongside Bootstrap -->
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            orbitron: ['Orbitron', 'sans-serif'],
            exo: ['"Exo 2"', 'sans-serif'],
            inter: ['Inter', 'system-ui', 'sans-serif'],
          },
          colors: {
            brand: {
              50: '#ecfeff',
              100: '#cffafe',
              200: '#a5f3fc',
              300: '#67e8f9',
              400: '#22d3ee',
              500: '#06b6d4',
              600: '#0891b2',
              700: '#0e7490',
              800: '#155e75',
              900: '#164e63',
            }
          }
        }
      }
    }
  </script>

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500;700;900&family=Exo+2:wght@400;600&family=Inter:wght@400;600&display=swap" rel="stylesheet">

  <style>
    /* Glass + glow animations */
    @keyframes glowPulse {
      0%   { box-shadow: 0 0 10px rgba(0, 200, 255, .25), 0 0 20px rgba(0, 120, 255, .15); }
      50%  { box-shadow: 0 0 25px rgba(0, 200, 255, .7),  0 0 55px rgba(0, 120, 255, .45); }
      100% { box-shadow: 0 0 10px rgba(0, 200, 255, .25), 0 0 20px rgba(0, 120, 255, .15); }
    }
    @keyframes pulseGlow {
      0%,100% { opacity:.45; transform: scale(1); }
      50%     { opacity:1;   transform: scale(1.06); }
    }
    @keyframes textGlow {
      0%   { text-shadow: 0 0 6px #00e0ff, 0 0 10px #1593ff; }
      50%  { text-shadow: 0 0 18px #00e0ff, 0 0 36px #1593ff; }
      100% { text-shadow: 0 0 6px #00e0ff, 0 0 10px #1593ff; }
    }
    .animate-glow      { animation: glowPulse 3s ease-in-out infinite; }
    .animate-pulseGlow { animation: pulseGlow 3s ease-in-out infinite; }
    .animate-textGlow  { animation: textGlow 3s ease-in-out infinite; }

    .glass {
      backdrop-filter: blur(10px);
      background: rgba(255,255,255,0.06);
      border: 1px solid rgba(255,255,255,0.10);
    }
  </style>
</head>

<body class="min-h-screen bg-gradient-to-br from-slate-900 via-blue-950 to-slate-800 text-white font-inter">
  <!-- Layout -->
  <div class="flex">

    <!-- Sidebar -->
    <aside class="w-72 hidden md:flex flex-col items-center py-8 shadow-xl bg-gradient-to-b from-slate-900 via-blue-900 to-slate-800">
      <!-- Logo block -->
      <div class="mb-10 relative flex flex-col items-center">
        <div class="relative">
          <!-- Use your own logo asset if available -->
          <img src="{{ url_for('static', filename='img/casepoint-logo.png') if false else 'https://upload.wikimedia.org/wikipedia/commons/3/39/Scales_of_Justice.svg' }}"
               alt="CasePoint Logo" class="w-20 h-20 animate-glow rounded-full p-2 bg-white/5">
          <div class="absolute inset-0 rounded-full blur-2xl bg-cyan-500/40 animate-pulseGlow"></div>
        </div>

        <!-- Font variations — pick ONE by toggling classes -->
        <h1 class="mt-3 text-2xl font-extrabold bg-gradient-to-r from-cyan-300 via-sky-400 to-indigo-300 bg-clip-text text-transparent animate-textGlow font-orbitron">
          CasePoint
        </h1>
        <p class="text-[0.75rem] text-cyan-200/90 tracking-wide">Legal Intelligence, Simplified by AI ⚡</p>

        <!-- If you want to try other fonts, swap the line above with one of these: -->
        <!-- <h1 class="mt-3 text-2xl font-extrabold bg-gradient-to-r from-cyan-300 via-sky-400 to-indigo-300 bg-clip-text text-transparent animate-textGlow font-exo">CasePoint</h1> -->
        <!-- <h1 class="mt-3 text-2xl font-extrabold bg-gradient-to-r from-cyan-300 via-sky-400 to-indigo-300 bg-clip-text text-transparent animate-textGlow font-inter tracking-wide">CasePoint</h1> -->
      </div>

      <nav class="w-full px-6 space-y-2 text-gray-200">
        {% set nav = [
          ('/dashboard','Dashboard','scale'),
          ('/search','Search Cases','search'),
          ('/acts','Acts & Rules','book'),
          ('/dictionary','Legal Dictionary','database'),
          ('/ai','AI Analysis','brain')
        ] %}
        {% for href,label,icon in nav %}
        <a href="{{ href }}" class="flex items-center gap-3 px-3 py-2 rounded-md hover:bg-white/10 hover:text-cyan-300 transition">
          <span class="w-5 h-5">
            {% if icon=='search' %}🔎{% elif icon=='book' %}📘{% elif icon=='database' %}💽{% elif icon=='brain' %}🧠{% else %}⚖️{% endif %}
          </span>
          <span>{{ label }}</span>
        </a>
        {% endfor %}
      </nav>
    </aside>

    <!-- Main -->
    <main class="flex-1 p-6 md:p-10 space-y-8">

      <!-- Hero -->
      <section class="glass rounded-3xl p-8 shadow-lg">
        <h2 class="text-3xl md:text-4xl font-extrabold">Welcome back, {{ user.full_name or 'John Doe' }} 👋</h2>
        <p class="mt-2 text-cyan-100/90">Access Pakistan’s most advanced AI-powered legal research platform.</p>
      </section>

      <!-- Quick Actions -->
      <section>
        <h3 class="text-xl font-semibold mb-4 text-gray-200">AI Quick Actions</h3>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          {% set actions = [
            ('/search','New Search','Start a new legal query','bg-gradient-to-r from-indigo-500 to-blue-600','🔍'),
            ('/laws/apply','Which Laws Apply','AI suggests applicable acts & rules','bg-gradient-to-r from-cyan-500 to-teal-500','⚖️'),
            ('/documents','Upload Acts','Add legal documents','bg-gradient-to-r from-purple-500 to-pink-500','⬆️')
          ] %}
          {% for href, title, desc, grad, emoji in actions %}
          <a href="{{ href }}" class="glass p-6 rounded-2xl hover:border-cyan-400 transition shadow-md">
            <div class="inline-block px-3 py-2 mb-3 rounded-xl text-white {{ grad }}">{{ emoji }}</div>
            <div class="text-lg font-semibold">{{ title }}</div>
            <div class="text-sm text-gray-300">{{ desc }}</div>
          </a>
          {% endfor %}
        </div>
      </section>

      <!-- Stats -->
      <section class="grid grid-cols-1 md:grid-cols-3 gap-6">
        <div class="glass rounded-2xl p-5">
          <div class="text-sm text-gray-300">Total Searches</div>
          <div class="text-3xl font-semibold mt-1">{{ stats.search_count }}</div>
        </div>
        <div class="glass rounded-2xl p-5">
          <div class="text-sm text-gray-300">Documents Available</div>
          <div class="text-3xl font-semibold mt-1">{{ "{:,}".format(stats.document_count) }}</div>
        </div>
        <div class="glass rounded-2xl p-5">
          <div class="text-sm text-gray-300">Plan Status</div>
          <div class="text-3xl font-semibold mt-1 capitalize">{{ user.subscription_tier or 'pro' }}</div>
        </div>
      </section>

      <!-- Recent Searches -->
      <section class="grid grid-cols-1 lg:grid-cols-2 gap-6">
        <div class="glass rounded-2xl p-6">
          <h3 class="text-lg font-semibold mb-3">Recent Searches</h3>
          <div class="space-y-3">
            {% for s in stats.recent_searches %}
            <div class="flex items-center justify-between p-3 bg-white/5 rounded-lg border border-white/10 hover:border-cyan-400 transition">
              <div>
                <div class="text-sm font-medium">{{ s.query }}</div>
                <div class="text-xs text-gray-300">{{ s.results }} results found</div>
              </div>
              <div class="text-xs text-gray-400">{{ s.date }}</div>
            </div>
            {% endfor %}
          </div>
        </div>

        <div class="glass rounded-2xl p-6">
          <h3 class="text-lg font-semibold mb-3">Subscription</h3>
          <div class="flex items-center justify-between py-2">
            <span class="text-sm text-gray-300">Current Plan</span>
            <span class="text-sm font-medium capitalize">{{ user.subscription_tier or 'pro' }}</span>
          </div>
          {% if (user.subscription_tier or 'pro') == 'free' %}
          <div class="mt-4 p-4 rounded-lg bg-yellow-500/10 border border-yellow-500/30 text-yellow-100">
            Upgrade to access advanced search and unlimited queries.
            <a href="/subscription" class="ml-2 underline">Upgrade →</a>
          </div>
          {% endif %}
          <div class="pt-4 mt-4 border-t border-white/10">
            <a href="/subscription" class="text-sm text-cyan-300 hover:text-cyan-200">Manage subscription →</a>
          </div>
        </div>
      </section>

    </main>
  </div>
</body>
</html>
